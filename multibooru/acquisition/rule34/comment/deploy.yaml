# This user is for the comment process
apiVersion: rabbitmq.com/v1beta1
kind: User
metadata:
  name: rule34-comment-writer
  namespace: imageboards
spec:
  rabbitmqClusterReference:
    name: message-broker
---
apiVersion: rabbitmq.com/v1beta1
kind: Permission
metadata:
  name: rule34-comment-writer-permission
  namespace: imageboards
spec:
  vhost: "/"
  userReference:
    name: "rule34-comment-writer"  # refers to name of User resource
  permissions:
    write: ".*"
    configure: ""
    read: ".*"
  rabbitmqClusterReference:
    name: message-broker
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: rule34-comment-writer
  namespace: imageboards
  labels:
    app: rule34-comment-writer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rule34-comment-writer
  template:
    metadata:
      labels:
        app: rule34-comment-writer
    spec:
      containers:
        - name: rule34-comment-writer
          image: registry.danya02.ru/danya02/imageboards/rule34-comment-writer:latest
          imagePullPolicy: Always
          env:
            - name: RUST_LOG
              value: debug
            - name: AMQP_SERVER
              value: message-broker
            - name: AMQP_USER
              valueFrom:
                secretKeyRef:
                  name: rule34-comment-writer-user-credentials
                  key: username
            - name: AMQP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rule34-comment-writer-user-credentials
                  key: password
            - name: PROXY_URL
              value: socks5://shadowsocks-client-service:1080